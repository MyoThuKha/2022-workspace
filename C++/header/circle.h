#include <cmath>
        
double area(double r)
{
    return (M_PI * r * r);
}

double parameter(double r)
{
    return (2 * M_PI * r);
}

double sphereArea(double r)
{
    return (4/3 * M_PI * r * r * r);
}
